---
// CloceIcon.astro

const { className = '', size = 24 } = Astro.props;
---

    <svg
      id="closeIcon"
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
        class={`${className} w-${size} h-${size} transition-transform duration-300`}

    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M6 18L18 6M6 6l12 12"
      />
    </svg>

