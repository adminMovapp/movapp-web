---
import { Picture } from 'astro:assets';

import Layout from '@layouts/Layout.astro';
import HackCard from '@components/HackCard.jsx';
import ImgElHack from '@assets/elhack-negro.png';

const cardHover =
   'border border-white/10 transition duration-300 ease-in-out hover:shadow-xl hover:-translate-y-1 hover:scale-105 cursor-pointer';
---

<Layout title="Tienda">
   <div class="container mx-auto px-4">
      <!-- Encabezado principal -->
      <div
         class="mt-32 mb-6 flex flex-col md:flex-row items-center justify-center space-y-4 md:space-y-0 md:space-x-4 text-center md:text-left"
         data-aos="fade-down"
      >
         <h2 class="text-lg md:text-3xl font-bold text-white">Protege tu información digital con</h2>
         <Picture src={ImgElHack} alt="Movapp Logo" class="w-28 md:w-32 h-auto" />
      </div>

      <!-- Descripción introductoria -->
      <div class="mt-6 flex items-center justify-center px-4" data-aos="fade-up">
         <p class="max-w-2xl text-sm md:text-lg text-justify text-white">
            En <span class="text-purple_mv font-bold">Movapp</span>, ofrecemos la protección que necesitas contra
            aplicaciones no reguladas que están comprometidas con tu seguridad. Con El Hack, podrás bloquear el acoso
            digital y recuperar el control de tu vida privada.
         </p>
      </div>

      <!-- Título de sección -->
      <h2 class="mt-6 text-lg md:text-2xl font-bold text-center text-white" data-aos="fade-up">Selecciona tus hacks</h2>

      <!-- Pasos -->

      <div class="mt-6 mx-auto flex flex-col max-w-2xl">
         <div data-aos="fade-up">
            <div class={`my-2 flex flex-col rounded-lg   p-6 shadow-md ${cardHover}`}>
               <h3 class="mb-2 text-left text-sm md:text-lg font-semibold text-white">Paso 1:</h3>
               <p class="text-white text-sm md:text-lg">
                  Si aún no lo has hecho, contáctanos por <span class="text-green-500 font-bold">WhatsApp</span>.
                  Evaluaremos si podemos ayudarte y te daremos indicaciones.
               </p>
            </div>
         </div>

         <div data-aos="fade-up">
            <div class={`my-2 flex flex-col rounded-lg   p-6 shadow-md ${cardHover}`}>
               <h3 class="mb-2 text-left text-sm md:text-lg font-semibold text-white">Paso 2:</h3>
               <p class="text-white text-sm md:text-lg">
                  Te canalizamos con un asesor que te indicará la cantidad de Hacks que debes adquirir.
               </p>
            </div>
         </div>

         <div data-aos="fade-up">
            <div class={`my-2 flex flex-col rounded-lg   p-6 shadow-md ${cardHover}`}>
               <h3 class="mb-2 text-left text-sm md:text-lg font-semibold text-white">Paso 3:</h3>
               <p class="text-white text-sm md:text-lg">Finaliza tu compra y vive El Hack hacia un nuevo inicio.</p>
            </div>
         </div>
      </div>
   </div>
   <!-- Componente dinámico -->
   <HackCard client:load />
</Layout>
