---
import Layout from '@layouts/Layout.astro';
---

<Layout title="Pago pendiente">
   <section class="mt-12 mb-20 flex flex-col items-center justify-center px-6 text-center" data-aos="fade-up">
      <!-- Icono de espera -->
      <div class="text-blue-500 mb-6">
         <svg
            xmlns="http://www.w3.org/2000/svg"
            class="w-20 h-20"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
         >
            <path
               stroke-linecap="round"
               stroke-linejoin="round"
               stroke-width="2"
               d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
         </svg>
      </div>

      <!-- Título -->
      <h1 class="text-3xl sm:text-4xl font-bold text-blue-500 mb-2">Tu pago está pendiente</h1>

      <!-- Mensaje -->
      <p class="text-lg text-gray-600 max-w-2xl mb-6">
         Hemos recibido tu intento de pago, pero aún no ha sido confirmado por la plataforma. Te notificaremos cuando se
         acredite correctamente.
      </p>

      <!-- Botones -->
      <div class="flex flex-col sm:flex-row gap-4">
         <a
            href="/"
            class="px-6 py-3 bg-purple_mv text-white rounded-lg font-semibold shadow hover:bg-purple-700 transition"
         >
            Volver al inicio
         </a>

         <!-- <a
        href="mailto:soporte@movapp.org"
        class="px-6 py-3 bg-gray-200 text-gray-800 rounded-lg font-semibold shadow hover:bg-gray-300 transition"
      >
        Contactar soporte
      </a> -->
      </div>
   </section>
</Layout>
