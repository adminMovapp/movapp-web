<div class="w-full bg-red-500 text-white text-center text-sm h-6 flex items-center justify-center mb-4 md:mb-5">
   <a
      href="${import.meta.env.PUBLIC_SHOW_HEADER_URL}"
      class="pulsing-text font-semibold underline hover:text-yellow-200 transition-colors duration-200"
      target="_blank"
      rel="noopener noreferrer"
   >
      Hoy transmisión en vivo !!
   </a>
</div>

<style>
   @keyframes pulse-text {
      0%,
      100% {
         opacity: 1;
      }
      50% {
         opacity: 0.5;
      }
   }

   .pulsing-text {
      animation: pulse-text 2s ease-in-out infinite;
   }
</style>
